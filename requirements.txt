# Ultra-minimal requirements for fast CI/CD
# =========================================
# Lightweight dependencies for CI builds and basic API testing
# This prevents heavy ML dependencies from slowing down CI

# Core API only
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.11.0
python-multipart==0.0.6

# Basic utilities
httpx==0.28.1
python-dotenv==1.0.0

# Testing
pytest==7.4.3

# Note: Heavy ML dependencies (torch, sklearn, pandas, neo4j, langchain, mlflow, etc.) 
# are in requirements-staging.txt for production deployment
# This keeps CI fast and reliable